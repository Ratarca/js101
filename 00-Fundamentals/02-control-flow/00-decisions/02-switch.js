let day = 1;

// Refactoring this
switch (day){
    case 1:
        console.log(`work`);
    case 2:
        console.log(`work`);
    case 3:
        console.log(`work`);
    case 5:
        console.log(`work`);
    case 6:
        console.log(`work`);
    default:
        console.log(`Not work`)
}

// Search about Pattern Matching,Enums, map collections to store procedures