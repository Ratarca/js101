// Objects is a composite data type that represents a collection of key-value pairs

dog_loop = {
    name:'loop',
    age:6,
    bark: () => {return console.log('AU AU')},
    walk: function (){
            return console.log('WALKING')
        }
}