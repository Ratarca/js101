/* 
CAC, LTV , Churn , ROI, Ticket medio, ROAS
TICKET MEDIO, cross sell, upsell, Fidelization, 
*/

let a = 10
let b = 100
let c = 200

// Math
let x = (a + b) * c
let y = x/a
let z = x - y

console.log(x, y, z)

/*Comparsion
    == , !=
    >, >=
    <, <=

*/
let equals = x == x
let diff = x != y
let greater = x <= y

console.log(equals, diff, greater)

// Logical : AND, OR, XOR

let and_logic = (true & (x!=y))
console.log(and_logic)